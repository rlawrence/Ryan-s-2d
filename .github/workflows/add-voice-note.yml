name: Add Voice Note

on:
  workflow_dispatch:
    inputs:
      transcription:
        description: "The transcribed text"
        required: true
        type: string
      title:
        description: "Title for the voice note (optional)"
        required: false
        type: string
      tags:
        description: "Comma-separated tags (optional)"
        required: false
        type: string
      source:
        description: "Source application (e.g. whisper, apple-shortcuts)"
        required: false
        type: string

permissions:
  contents: write

jobs:
  add-voice-note:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Save transcription
        env:
          TRANSCRIPTION: ${{ inputs.transcription }}
          NOTE_TITLE: ${{ inputs.title }}
          NOTE_TAGS: ${{ inputs.tags }}
          NOTE_SOURCE: ${{ inputs.source }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          ARGS=()
          if [ -n "$NOTE_TITLE" ]; then
            ARGS+=(--title "$NOTE_TITLE")
          fi
          if [ -n "$NOTE_TAGS" ]; then
            ARGS+=(--tags "$NOTE_TAGS")
          fi
          if [ -n "$NOTE_SOURCE" ]; then
            ARGS+=(--source "$NOTE_SOURCE")
          fi
          RESULT=$(printf '%s' "$TRANSCRIPTION" | python scripts/save_transcription.py --stdin "${ARGS[@]}")
          echo "$RESULT"
          echo "SAVED_FILE=$RESULT" >> "$GITHUB_ENV"

      - name: Commit and push
        run: |
          git config user.name "Voice Note Bot"
          git config user.email "voice-bot@users.noreply.github.com"
          git add voice-notes/
          FILENAME=$(basename "${SAVED_FILE#Saved: }" .md)
          git commit -m "Add voice note: $FILENAME"
          git push
